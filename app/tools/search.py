class SearchTool:
    """
    Tool for searching external information (e.g. Google Search).
    """
    async def search(self, query: str) -> str:
        # Placeholder
        return f"Results for {query}"
