version: '3.8'

services:
  api:
    build:
      context: ../../
      dockerfile: infra/docker/Dockerfile
    container_name: llm-wrapper-api
    ports:
      - "8000:8000"
    env_file:
      - ../../.env
    volumes:
      - ../../app:/app/app
      - ../../data:/app/data
    restart: always
